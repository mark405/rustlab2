[package]
name = "Lab2"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.0-rc.2"
serde = { version = "1.0", features = ["derive"] }  # Для серіалізації даних
serde_json = "1.0"  # Якщо використовуєте JSON